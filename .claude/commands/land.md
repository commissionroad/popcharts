---
description: Land a PR — merge, update base branch, clean up branch/worktree
argument-hint: [pr-number | pr-url | branch]
---

Follow `skills/engineering/land/SKILL.md` exactly — it is the single source
of truth; do not re-implement the landing steps by hand.

Target: $ARGUMENTS — a PR number, PR URL, or branch. If empty, land the PR
for the current branch (the script refuses to run from main).
