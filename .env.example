# Pop Charts local environment template.
#
# The fixture-backed app and protocol tests do not require environment variables
# for the default local workflow yet. Copy this file to the package-specific
# location that needs it as integrations land:
#
# - app/.env.local
# - protocol/.env
#
# Keep real secrets out of git.

