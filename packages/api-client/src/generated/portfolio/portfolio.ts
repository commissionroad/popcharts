/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Pop Charts API
 * Read API for Pop Charts indexed market events.
 * OpenAPI spec version: 0.1.0
 */
import type { GetPortfolioParams, Portfolio } from ".././models";

/**
 * Returns one wallet's cross-market lifecycle view: pre-graduation receipts joined to their settlement results, graduated YES/NO outcome-token positions (held wallet balance plus tokens committed to the wallet's own resting ask orders, valued at the current pool price when available), and open venue maker orders. Public owner-scoped read; no authentication.
 * @summary Get a wallet's full portfolio
 */
export type getPortfolioResponse200 = {
  data: Portfolio;
  status: 200;
};

export type getPortfolioResponse400 = {
  data: string;
  status: 400;
};

export type getPortfolioResponseSuccess = getPortfolioResponse200 & {
  headers: Headers;
};
export type getPortfolioResponseError = getPortfolioResponse400 & {
  headers: Headers;
};

export type getPortfolioResponse =
  | getPortfolioResponseSuccess
  | getPortfolioResponseError;

export const getGetPortfolioUrl = (chainId: string, params: GetPortfolioParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/portfolio/${chainId}?${stringifiedParams}`
    : `/portfolio/${chainId}`;
};

export const getPortfolio = async (
  chainId: string,
  params: GetPortfolioParams,
  options?: RequestInit
): Promise<getPortfolioResponse> => {
  const res = await fetch(getGetPortfolioUrl(chainId, params), {
    ...options,
    method: "GET",
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: getPortfolioResponse["data"] = body ? JSON.parse(body) : {};
  return { data, status: res.status, headers: res.headers } as getPortfolioResponse;
};
