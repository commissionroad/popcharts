/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Pop Charts API
 * Read API for Pop Charts indexed market events.
 * OpenAPI spec version: 0.1.0
 */
import type { ResolutionCheckAccepted, ResolutionCheckRefused } from ".././models";

/**
 * Public early-resolution nudge for a graduated market past its earliest resolution time — the resolution sibling of the graduation failsafe (repo ADR 0024). Queues one resolver evaluation; the AI resolver still decides the outcome and the on-chain dispute window still guards it, so the endpoint is safe unauthenticated. Cost is bounded by a per-market 24-hour cooldown rather than caller identity: repeat requests inside the window are refused (409) with the next eligible time.
 * @summary Request a resolution check
 */
export type requestMarketResolutionCheckResponse200 = {
  data: ResolutionCheckAccepted;
  status: 200;
};

export type requestMarketResolutionCheckResponse400 = {
  data: string;
  status: 400;
};

export type requestMarketResolutionCheckResponse404 = {
  data: string;
  status: 404;
};

export type requestMarketResolutionCheckResponse409 = {
  data: ResolutionCheckRefused;
  status: 409;
};

export type requestMarketResolutionCheckResponseSuccess =
  requestMarketResolutionCheckResponse200 & {
    headers: Headers;
  };
export type requestMarketResolutionCheckResponseError = (
  | requestMarketResolutionCheckResponse400
  | requestMarketResolutionCheckResponse404
  | requestMarketResolutionCheckResponse409
) & {
  headers: Headers;
};

export type requestMarketResolutionCheckResponse =
  | requestMarketResolutionCheckResponseSuccess
  | requestMarketResolutionCheckResponseError;

export const getRequestMarketResolutionCheckUrl = (
  chainId: string,
  marketId: string
) => {
  return `/markets/${chainId}/${marketId}/resolution-check`;
};

export const requestMarketResolutionCheck = async (
  chainId: string,
  marketId: string,
  options?: RequestInit
): Promise<requestMarketResolutionCheckResponse> => {
  const res = await fetch(getRequestMarketResolutionCheckUrl(chainId, marketId), {
    ...options,
    method: "POST",
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: requestMarketResolutionCheckResponse["data"] = body
    ? JSON.parse(body)
    : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  } as requestMarketResolutionCheckResponse;
};
