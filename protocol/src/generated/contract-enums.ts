// This file is generated by scripts/export-contract-metadata.ts.
// Do not edit it directly.
//
// Numeric encodings of the Solidity enums off-chain code decodes. Enums have
// no ABI representation — a `Status` argument compiles to `uint8` and its
// member names and ordinals are erased — so these tables are derived from the
// solc AST in artifacts/build-info instead, and regenerate with the contracts.
//
// Enums are append-only: ordinals follow declaration order, which is not
// necessarily lifecycle order. Reordering members is a breaking on-chain
// change, not a formatting choice.

/** Solidity member names of `MarketTypes.MarketStatus`, in ordinal order. */
export const MARKET_STATUS_MEMBERS = [
  "Active",
  "Frozen",
  "Graduating",
  "Graduated",
  "Refunded",
  "Resolved",
  "Cancelled",
] as const;

/**
 * `MarketTypes.MarketStatus` numeric encoding.
 * Lifecycle status of a pregrad market held by the PregradManager singleton.
 */
export const MARKET_STATUS = {
  active: 0,
  frozen: 1,
  graduating: 2,
  graduated: 3,
  refunded: 4,
  resolved: 5,
  cancelled: 6,
} as const;

/** A `MarketTypes.MarketStatus` contract encoding. */
export type MarketStatusCode = (typeof MARKET_STATUS)[keyof typeof MARKET_STATUS];

/** Solidity member names of `CompleteSetBinaryMarket.Status`, in ordinal order. */
export const POSTGRAD_MARKET_STATUS_MEMBERS = [
  "Trading",
  "Resolved",
  "Cancelled",
  "ResolutionPending",
  "Disputed",
] as const;

/**
 * `CompleteSetBinaryMarket.Status` numeric encoding.
 * Lifecycle status of a graduated complete-set market, dispute states included (protocol ADR 0013).
 */
export const POSTGRAD_MARKET_STATUS = {
  trading: 0,
  resolved: 1,
  cancelled: 2,
  resolutionPending: 3,
  disputed: 4,
} as const;

/** A `CompleteSetBinaryMarket.Status` contract encoding. */
export type PostgradMarketStatusCode =
  (typeof POSTGRAD_MARKET_STATUS)[keyof typeof POSTGRAD_MARKET_STATUS];
