# Integration Boundaries

This directory holds adapters for wallet, contracts, indexers, analytics, and
future external services. Keep domain logic out of adapters; parse external data
into typed app shapes before it enters features.
