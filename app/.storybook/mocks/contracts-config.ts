/**
 * Preview stub for the chain-config module: the panel only reads the chain id,
 * and the real module pulls in viem plus NEXT_PUBLIC env parsing that has no
 * meaning inside Storybook. The value is irrelevant to the render — positions
 * are matched by market id, not chain id.
 */
export const configuredPopChartsChainId = 31337;
